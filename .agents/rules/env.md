---
trigger: always_on
---

# Environment Configuration
When executing Python commands, ALWAYS read the local `.env` file and use the interpreter located at `./.venv/bin/python` (or `.\.venv\Scripts\python.exe` on Windows).